#! /usr/bin/env bash

INPUT_DECK_DIR=`pwd`

cd @abs_srcdir@
@CXX@ @DEFS@ @CPPFLAGS@ @CXXFLAGS@ @EXTRA_CPPFLAGS@ -Isrc/include src/main.cxx src/deck_wrapper.cxx -DINPUT_DECK=$INPUT_DECK_DIR/$1 -o $INPUT_DECK_DIR/$1.@EXTENSION@ -L. @EXTRA_LDFLAGS@ @EXTRA_LIBS@ lib/.libs/libvpic.a -lm

#CXXLINK = $(LIBTOOL) --tag=CXX --mode=link $(CXXLD) $(AM_CXXFLAGS) \
#    $(CXXFLAGS) $(AM_LDFLAGS) $(LDFLAGS) -o $@
