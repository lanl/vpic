#include <sort_p_overlay.h>
#include <stdio.h>

void _SPUEAR_sort_p_pipeline_spu( uint64_t args,
                    uint32_t pipeline_rank,
					uint32_t n_pipeline) {
  fprintf(stderr, "In sort_p\n");
  fflush(stderr);
} // sort_p
